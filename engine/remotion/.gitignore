node_modules/
out/
.env

# áudio: sample de voz (NUNCA versionar) + WAVs de narração (pesados, regeneráveis).
# durations.json (metadados, ~bytes) FICA versionado — timing.ts importa no build.
audio/voice/
*.wav
*.mp3
